<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cinema Booking</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            font-family: 'Segoe UI', sans-serif;
            background: linear-gradient(120deg, #1e3c72, #2a5298);
            color: white;
            min-height: 100vh;
            margin: 0;
            padding-bottom: 2rem;
        }

        .container {
            padding: 2rem;
        }

        h1 {
            font-size: 2.5rem;
            margin-bottom: 1rem;
        }

        .movie-card {
            transition: transform 0.3s, box-shadow 0.3s;
            height: 100%;
            background: rgba(0, 0, 0, 0.4) !important;
            border: 1px solid rgba(255, 255, 255, 0.1) !important;
        }

        .movie-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.3);
        }

        .movie-image {
            height: 400px;
            object-fit: cover;
            border-bottom: 1px solid rgba(255, 255, 255, 0.1);
        }

        .card-body {
            padding: 1.5rem;
        }

        .movie-description {
            font-size: 0.9rem;
            opacity: 0.8;
            margin-bottom: 1rem;
            height: 80px;
            overflow: hidden;
            text-overflow: ellipsis;
            display: -webkit-box;
            -webkit-line-clamp: 3;
            -webkit-box-orient: vertical;
        }

        .price-tag {
            font-size: 1.2rem;
            font-weight: bold;
            color: #00b894;
            margin-bottom: 1rem;
        }

        .book-btn {
            background-color: #00b894;
            border: none;
            padding: 0.8rem;
            font-weight: 600;
            letter-spacing: 0.5px;
            transition: all 0.3s ease;
        }

        .book-btn:hover {
            background-color: #019875;
            transform: translateY(-2px);
        }

        .navbar {
            background: rgba(0, 0, 0, 0.2) !important;
            backdrop-filter: blur(10px);
        }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark fixed-top">
        <div class="container">
            <a class="navbar-brand" href="/">🎬 Cinema Booking</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="/admin">User Panel</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <div class="container" style="margin-top: 80px;">
        <h1 class="text-center mb-5">Now Showing</h1>
        
        <div class="row g-4">
            <% movies.forEach(movie => { %>
                <div class="col-md-3">
                    <div class="movie-card card text-light">
                        <img src="<%= movie.image %>" class="movie-image" alt="<%= movie.name %>" onerror="this.src='/images/default-movie.jpg'">
                        <div class="card-body">
                            <h5 class="card-title mb-3"><%= movie.name %></h5>
                            <p class="movie-description"><%= movie.description %></p>
                            <div class="price-tag">₹<%= movie.price %></div>
                            <a href="/book?movie=<%= movie.id %>" class="btn btn-primary book-btn w-100">Book Now</a>
                        </div>
                    </div>
                </div>
            <% }); %>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html> 